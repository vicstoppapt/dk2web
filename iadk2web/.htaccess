# Security: Prevent direct access to config file
<Files "config.php">
    Order Allow,Deny
    Deny from all
</Files>

# Prevent directory listing
Options -Indexes

# Protect sensitive files
<FilesMatch "\.(sql|log|bak|backup|old|tmp)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

